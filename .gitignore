# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Testing / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Large generated search output (commit curated certificates under data/ explicitly)
/scratch/
/output/
*.tmp

# Lean / lake build artifacts
.lake/
*.olean

# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz

# 10^12 R-sequence array (304 MB, exceeds GitHub's 100 MB file limit).
# Regenerate deterministically with scripts/run_1e12.sh; the SHA-256 is
# pinned in data/hard_primes_1e12_minimalR.meta.json, and the archived
# release carries the file itself.
data/hard_primes_1e12_minimalR.rvals.u8.gz
